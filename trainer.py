import torch
import numpy as np

class Trainer():

    def __init__(self, generator, discriminator) -> None:
        pass


    def fit(dataloader):
        # fit generator and discriminator to training data
        self.generator.train()
        self.discriminator.train()


    def validate():
        # validate the hyperparameters
        self.generator.eval()
        self.discriminator.eval()


    def test():
        # test the trained generator
        self.generator.eval()
        self.discriminator.eval()


    def inference():
        # use the generator for inference
        pass
